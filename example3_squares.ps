# Perfect Squares Generator
pattern perfect_squares(offset) {
    if (n >= 1) {
        num = offset + n;
        square = num * num;
        print square;
    }
}

generate perfect_squares(0): 20;